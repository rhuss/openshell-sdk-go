# Implementation Plan: Gateway Config Convenience Layer

**Branch**: `017-gateway-config` | **Date**: 2026-07-02 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `specs/017-gateway-config/spec.md`

## Summary

Add an `openshell/v1/gateway/` package that reads the Rust CLI's on-disk
gateway configurations and constructs fully wired SDK clients. The package
resolves XDG config paths, validates gateway names, loads tokens lazily,
maps auth modes to existing auth providers, and provides one-call
convenience constructors. This eliminates 20+ lines of boilerplate for
Go programs connecting to gateways managed by the Rust CLI.

## Technical Context

**Language/Version**: Go 1.21+ (matches go.mod)
**Primary Dependencies**: stdlib (os, path/filepath, encoding/json, errors), golang.org/x/oauth2 (for OIDC token source)
**Storage**: Filesystem (read-only, XDG config directories)
**Testing**: Go testing + testify (assert/require), filesystem-based test fixtures
**Target Platform**: Linux/macOS (Windows out of scope for v1)
**Project Type**: Library (Go SDK package)
**Performance Goals**: Config loading under 10ms (filesystem reads only, no network)
**Constraints**: Read-only filesystem access, no new external dependencies beyond oauth2
**Scale/Scope**: Dozens of gateways in config directories (small N)

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

| Principle | Status | Notes |
|-----------|--------|-------|
| I. Proto Isolation | PASS | No proto types involved. Gateway package reads JSON files. |
| II. Idiomatic Go | PASS | Functional options, error returns, context propagation. |
| III. Test-First | PASS | All public functions get tests. Filesystem fixtures for testing. |
| IV. Upstream Tracking | N/A | No proto changes needed. |
| V. Minimal Dependencies | PASS | Only stdlib + oauth2 (already a dependency). No new deps. |
| VI. Secrets Never Leak | PASS | Tokens never in error messages. Error types omit credentials. |
| VII. Deep Copy at Boundaries | PASS | Config is immutable snapshot. No shared mutable state. |
| VIII. Doc Examples Compile | PASS | Package doc.go with runnable examples. |
| IX. Agent-Friendly Docs | PASS | All exported types/functions documented. Error codes listed. |
| X. Proto-SDK Naming Fidelity | N/A | No proto counterparts for gateway types. |
| XI. Fake-Real Parity | N/A | No fake implementation needed (filesystem-based package). |
| XII. Graceful Shutdown | N/A | No connections managed by gateway package itself. |
| XIII. Docs Accompany Features | PASS | README update + doc.go in same PR. |

## Project Structure

### Documentation (this feature)

```text
specs/017-gateway-config/
├── plan.md              # This file
├── spec.md              # Feature specification
├── research.md          # Research findings
├── data-model.md        # Entity definitions
├── contracts/           # Public API contracts
│   └── gateway-api.md   # Exported functions and types
└── tasks.md             # Task breakdown (generated by /speckit-tasks)
```

### Source Code (repository root)

```text
openshell/v1/gateway/
├── doc.go               # Package documentation with examples
├── config.go            # Config, Info, AuthMode, ConfigSource types
├── config_test.go       # Config type tests
├── errors.go            # Sentinel error types
├── errors_test.go       # Error type tests
├── gateway.go           # NewClient, LoadConfig, ListGateways
├── gateway_test.go      # Main function tests
├── options.go           # ClientOption functional options
├── options_test.go      # Options tests
├── paths.go             # XDG resolution, name validation, directory scanning
├── paths_test.go        # Path resolution tests
├── token.go             # Lazy token loading, diskTokenSource
└── token_test.go        # Token loading tests
```

**Structure Decision**: Single flat package under `openshell/v1/gateway/`.
No sub-packages needed given the focused scope. Test files use filesystem
fixtures (temp directories with metadata.json and token files) rather
than mocks, matching the Rust CLI's test approach.

## Global Constraints

These constraints apply to every task implicitly:

- **Go version**: 1.21+ (matches go.mod)
- **Platform**: Linux/macOS only. Windows out of scope for v1.
- **Dependencies**: No new external dependencies beyond `golang.org/x/oauth2` (already in go.mod). stdlib only otherwise.
- **SPDX header**: Every new `.go` file must start with the project's SPDX license header.
- **Linter**: All code must pass `golangci-lint` via `make lint`.
- **Secrets**: Tokens, credentials, and API keys must never appear in error messages, logs, or string representations.

## Complexity Tracking

No constitution violations to justify.
